[package]
name = "music_core"
version = "0.1.0"
edition = "2021"

[lib]
name = "music_core"
crate-type = ["cdylib"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
walkdir = "2.5"
